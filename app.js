(()=>{const c=window.STARFALL_CONFIG||{},games=c.games||[],updates=c.updates||[],$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");function stars(){const l=$("#stars");let h="";for(let i=0;i<70;i++){h+=`<i style="left:${Math.random()*100}%;top:${Math.random()*100}%;width:${1+Math.random()*2}px;height:${1+Math.random()*2}px;animation-delay:${Math.random()*4}s;opacity:${.2+Math.random()*.7}"></i>`}l.innerHTML=h}function render(filters="All"){const t=$("#gameGrid");t.innerHTML=games.filter(g=>filters==="All"||(g.categories||[]).includes(filters)).map(g=>`<article class="game-card ${g.url?"":"future"}"><div class="game-art"><span class="icon">${esc(g.icon)}</span><span class="status ${g.status==='live'?'live':''}">${g.status==='live'?'LIVE':'COMING SOON'}</span></div><div class="game-body"><div class="chips">${(g.categories||[]).slice(0,3).map(x=>`<span>${esc(x)}</span>`).join("")}</div><h3>${esc(g.title)}</h3><p>${esc(g.description)}</p><div class="game-footer"><span>${g.version?esc(g.version):''}</span>${g.url?`<a class="button small primary" target="_blank" rel="noreferrer" href="${esc(g.url)}">Play</a>`:'<span>In development</span>'}</div></div></article>`).join("")}function setupFilters(){const cats=["All",...new Set(games.flatMap(g=>g.categories||[]))],f=$("#filters");f.innerHTML=cats.map((x,i)=>`<button class="${i===0?'active':''}" data-filter="${esc(x)}">${esc(x)}</button>`).join("");f.addEventListener('click',e=>{const b=e.target.closest('[data-filter]');if(!b)return;$$('#filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)})}function renderUpdates(){$('#updateGrid').innerHTML=updates.map(u=>`<article class="update-card"><div class="update-meta"><span>${esc(u.date)}</span><span>${esc(u.game)}</span><span>${esc(u.type)}</span></div><h3>${esc(u.title)}</h3><p>${esc(u.body)}</p></article>`).join('')}function nav(){const b=$('#navToggle'),n=$('#mainNav');b.addEventListener('click',()=>n.classList.toggle('open'));n.addEventListener('click',e=>{if(e.target.closest('a'))n.classList.remove('open')})}$('#year').textContent=new Date().getFullYear();stars();setupFilters();render();renderUpdates();nav()})();